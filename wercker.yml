box: wercker-labs/docker
build:
  steps:
    - script:
        name: docker version
        code: |
          docker -v
  after-steps:
    - wantedly/pretty-slack-notify:
        webhook_url: $SLACK_WEBHOOK_URL
        channel: ops
        username: wercker
