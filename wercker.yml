box: wercker-labs/docker
build:
  steps:
    - script:
        name: docker version
        code: |
          docker -v
  after-steps:
    - wantedly/pretty-slack-notify:
        team: voxxit
        token: $SLACK_TOKEN
        channel: ops
        username: wercker
